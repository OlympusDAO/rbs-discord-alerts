query LatestPriceSnapshot {
  priceSnapshots(orderBy: block, orderDirection: desc, first: 1) {
    block
    date
    gOhmUsdPrice
    id
    ohmUsdPrice
    ohmUsdPrice1dDelta
    ohmUsdPrice30dVolatility
    timestamp
  }
}
